[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/mcp/http-entry.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "always"
numReplicas = 1
